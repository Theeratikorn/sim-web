import React from 'react'
import ReactDOM from 'react-dom/client'
import SimApp from './SimApp.jsx'

ReactDOM.createRoot(document.getElementById('sim-root')).render(
  <React.StrictMode>
    <SimApp />
  </React.StrictMode>,
)
